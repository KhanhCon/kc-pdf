# KhanhCon.github.io


initial upload: 
todos: 
- Main preview scrollable
- Don't try to load pdfs dynamically but block the app while loading until the whole file is loaded (including thumbnails or thumbnails can be loaded later?)
- split/tach button (no toggle)
- Show full page preview before split export
- Exporting as normally saving files ( show where to save file then write name under) (Currently it only asks for name then save it in the "Downloads" folder)
- "merge preview" should allow joom in and out like viewing pdfs with google chrome.
